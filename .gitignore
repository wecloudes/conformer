# local env / secrets
compose/.env
compose/caddy-root.crt

# terraform / opentofu
**/.terraform/
**/.terraform.lock.hcl
*.tfstate
*.tfstate.*
tfplan

# terragrunt
**/.terragrunt-cache/

# transient module checkouts (Model B example)
examples/consumer-side-mapotf/upstream/

# build / patch artifacts
*.zip
*.tf.mptfbackup
registry-api/registry-api

# code intelligence index (local, not source)
.codegraph/

# OS
.DS_Store

# input design slides (not source)
Screenshot*.png
